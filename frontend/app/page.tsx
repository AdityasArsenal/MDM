import {Button as Wwww} from "./components/ui/button";

export default function Home() {
  const counter = 0;

  return (
   <div>
    <h1>Hello World</h1>
    <p>Counter: {counter}</p>
    {/* <button onClick={() => setCounter(counter + 1)}>Increment</button> */}
    <Wwww />
   </div>
  );
}
